// StartSpace background service worker (Manifest V3).
// Currently a no-op placeholder — lifecycle hooks go here as the product grows.

chrome.runtime.onInstalled.addListener(() => {
  // Future: seed default config, migrate stored data, etc.
});
